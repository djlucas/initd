[Unit]
Description=Samba AD Daemon
After=network.target

[Service]
Type=forking
EnvironmentFile=@SYSCONFIG_DIR@/samba
PIDFile=/run/samba.pid
ExecStart=@SBINDIR@/samba $SAMBA_OPTS
ExecReload=@BINDIR@/kill -HUP $MAINPID

[Install]
WantedBy=multi-user.target

