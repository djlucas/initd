[Unit]
Description=Subversion protocol daemon
After=network.target

[Service]
Type=forking
EnvironmentFile=@SYSCONFIG_DIR@/svnserve
ExecStart=@BINDIR@/svnserve --daemon --pid-file=/run/svnserve/svnserve.pid $SVNSERVE_OPTS

[Install]
WantedBy=multi-user.target
