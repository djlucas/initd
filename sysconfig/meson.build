# Sysconfig files installation is handled by:
# - units/meson.build (reference configs via install-reference target)
# - units/optional/meson.build (optional service configs via install-<service> targets)
